# ============================================================================
# Stream Aggregator API - Environment Variables Template
# 
# 1. Copy this file to .env
# 2. Fill in your actual values
# 3. Keep .env in .gitignore (already configured)
# ============================================================================

# Application Settings
APP_NAME="Stream Aggregator API"
APP_VERSION="1.0.0"
DEBUG=false

# Server Settings
HOST="0.0.0.0"
PORT=8000

# CORS Settings (comma-separated origins for production)
ALLOWED_ORIGINS="http://localhost:3000,https://your-frontend-domain.vercel.app"

# Supabase Database Settings
SUPABASE_URL="https://your-project-ref.supabase.co"
SUPABASE_ANON_KEY="your-anon-public-key-here"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-secret-key-here"
SUPABASE_JWT_SECRET="your-jwt-secret-here"

# PostgreSQL Direct Connection (optional)
POSTGRES_URL="postgresql://user:pass@host:port/db"
POSTGRES_URL_NON_POOLING="postgresql://user:pass@host:port/db"

# OAuth Settings - Managed via admin interface
# (Twitch/YouTube credentials are stored in system_settings table)

# Security Settings
SECRET_KEY="your-super-secret-key-for-jwt-signing-min-32-chars"
ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 days

# API Settings
API_V1_STR="/api"

# ============================================================================
# Development vs Production Notes:
# 
# Development:
# - DEBUG=true
# - ALLOWED_ORIGINS="*" or "http://localhost:3000"
# - Use Supabase development project
# 
# Production:
# - DEBUG=false  
# - ALLOWED_ORIGINS="https://your-domain.com"
# - Use Supabase production project
# - Ensure SECRET_KEY is cryptographically secure
# ============================================================================